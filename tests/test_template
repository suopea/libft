#include <stdio.h>
#include <limits.h>
#include <ctype.h>
#include <string.h>
#include "../libft.h"

void	test(input)
{
	if (ft_FUNCTION() == FUNCTION())
	{
		printf("success\n");
	}
	else
	{
		printf("FAILED :(\n");
	}
}

int main(void)
{
	test();
	return(0);	
}