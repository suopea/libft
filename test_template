#include <stdio.h>
#include <limits.h>
#include <ctype.h>
#include "libft.h"

void	test(input)
{
	if (ft_FUNCTION() == (0 != FUNCTION()))
	{
		printf("success\n");
	}
	else
	{
		printf("FAILED :(\n");
	}
}

int main(void)
{
	test();
	return(0);	
}